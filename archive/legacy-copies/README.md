# Archived Legacy Copies

This directory preserves non-canonical root-level copies created by filename
collisions, such as `README (1).md` and `config (2).yml`.

These files are retained for traceability but are not part of the active
repository surface. The canonical file keeps the unsuffixed name in the
repository root.